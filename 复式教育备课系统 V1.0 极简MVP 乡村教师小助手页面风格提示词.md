# 复式教育备课系统 V1\.0 极简MVP 乡村教师小助手页面风格提示词

核心原则：贴合乡村教师使用习惯（中高龄教师适配、操作直观、无复杂装饰），适配PC网页端\+手机H5自适应，极简实用、易读易懂，不追求花哨设计，降低视觉干扰，同时贴合教育类产品沉稳、亲切的调性，适配1人月开发周期（风格统一，减少前端开发成本）。

## 一、配色要求（柔和不刺眼，适配乡村教师视觉习惯）

1. **主色调**：柔和浅草绿（\#E6F4EA），作为页面背景色、按钮主色，贴合教育场景，视觉温和不刺眼，长时间查看不疲劳；

2. **辅助色**：浅米白（\#F8F7F4）（表单、列表背景）、深灰色（\#333333）（正文文字）、浅灰色（\#666666）（辅助文字）、淡红色（\#F5C6C6）（未备课状态、提示警告）、淡绿色（\#C8E6C9）（已备课状态）；

3. **配色禁忌**：禁止使用鲜艳刺眼颜色（如亮红、亮黄、深紫），禁止多色渐变、复杂纹理，禁止配色超过4种（主色\+辅助色），确保视觉统一；

4. **特殊适配**：按钮hover状态仅加深主色亮度（\#D4E9D9），无其他复杂效果；提示文字（必填、错误）用淡红色，清晰醒目但不刺眼。

## 二、字体要求（易读性优先，适配中高龄教师）

1. **字体选择**：优先使用系统默认字体（Windows：微软雅黑、宋体；Mac：苹方），无需引入外部字体，降低开发成本，同时保证不同设备显示一致，字体清晰无模糊；

2. **字体禁忌**：禁止使用小众字体、艺术字体、斜体（除特殊强调外），禁止使用细体，确保文字清晰易认；

3. **字号规范**（适配PC\+手机H5，手机端自动放大10%\-20%）：
        

    - 页面标题（如“个人备课列表页”）：18px\-22px，加粗；

    - 模块标题（如“模块1：极简账号体系”）：16px\-18px，加粗；

    - 正文文字（表单提示、列表内容、说明文字）：14px\-16px，常规粗细；

    - 按钮文字、表单输入文字：16px，加粗；

    - 辅助文字（规则提示、版权信息）：12px\-13px，常规粗细，浅灰色（\#666666）。

## 三、页面风格要求（极简实用，接地气，无冗余装饰）

1. **整体风格**：简约、干净、接地气，贴合乡村教师操作习惯，无复杂动画、无弹窗遮罩、无多余装饰（如图标、边框纹理），仅保留核心功能元素；

2. **元素风格**：
        

    - 按钮：圆角2px（极简圆角，不夸张），纯色填充，无阴影、无渐变，文字居中，大小统一（PC端：80px\*36px，手机端自适应放大）；

    - 表单：无边框或细灰色边框（\#E0E0E0），背景浅米白，输入框内文字居中对齐（适配中高龄教师输入查看）；

    - 列表：无分隔线，仅用上下留白区分条目，关键信息（如备课状态）加粗高亮，无多余图标；

    - 提示信息：纯文字提示，无图标，位置在对应元素下方，字体12px，淡红色（错误/警告）、深灰色（常规提示）。

3. **交互风格**：无复杂交互，仅保留“点击”“输入”两种核心交互，按钮点击无动画，仅提示“已点击”（文字提示或颜色轻微变化），避免hover、滑动等复杂效果，降低开发成本。

## 四、排版要求（清晰有序，方便快速查找功能）

1. **整体排版**：居中对齐，左右留白充足（PC端左右各留白10%，手机端左右各留白5%），避免内容拥挤，方便快速浏览；

2. **内容排版**：
        

    - 标题：居中或左对齐（页面标题居中，模块标题、表单标题左对齐），与下方内容留白15px\-20px；

    - 表单：按“标签\-输入框”上下排列，标签在上，输入框在下，标签与输入框留白5px，不同表单之间留白10px；

    - 列表：按垂直方向排列，条目之间留白10px，关键信息（如班级名称、课程名称）左对齐，辅助信息（如备课状态）右对齐；

    - 按钮：统一放在页面底部或表单下方，左右对齐（多个按钮时，间距10px），优先按“确认/下一步”在右、“取消/上一步/返回”在左的顺序排列，符合乡村教师操作习惯；

    - 表格：无边框，表头加粗，内容居中对齐，行与行之间留白5px，表头与内容之间用浅灰色细线条分隔（可选），确保表格内容清晰易读。

3. **自适应排版**：手机H5端自动适配屏幕宽度，表单、按钮、文字自动放大，避免横向滚动，核心内容（如输入框、按钮）优先展示，不遮挡关键功能。

## 五、特殊适配要求（贴合乡村教师使用场景）

1. 易读性适配：文字对比度≥4\.5:1（深灰色文字\+浅背景），避免浅色文字配浅色背景，确保中高龄教师清晰查看；

2. 操作适配：按钮尺寸适中，PC端按钮最小尺寸80px\*36px，手机端按钮最小尺寸60px\*30px，避免过小难以点击；

3. 内容适配：页面无冗余文字，说明文字简洁明了（不超过20字），避免专业术语，用乡村教师易懂的语言（如“未备课”“已备课”，不使用“待完成”“已完成”）；

4. 开发适配：所有风格要求均贴合1人月开发周期，不增加前端开发成本，无需复杂样式、外部资源引入，确保快速落地。

## 六、核心风格总结提示词

极简实用、接地气、易读易懂，柔和浅草绿主色调，微软雅黑/宋体清晰字体，字号适中（正文14\-16px），居中排版、留白充足，无复杂装饰、无多余交互，按钮统一、表单简洁，适配中高龄乡村教师，贴合教育场景，适配PC\+手机H5自适应，低成本开发落地。

> （注：文档部分内容可能由 AI 生成）
