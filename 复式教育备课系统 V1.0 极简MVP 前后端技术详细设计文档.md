# 复式教育备课系统 V1\.0 极简MVP 前后端技术详细设计文档

## 一、技术选型核心原则

1. **极致适配 1 人月开发周期**：优先选择成熟、低代码、开发效率高的主流技术栈，剔除所有冗余设计与非刚需组件，保障单人全栈快速落地；

2. **完全匹配业务边界**：严格贴合 MVP 功能范围，不涉及已剔除的 AI、外部数据源、多角色管理等功能，仅聚焦核心业务闭环；

3. **贴合使用场景**：适配乡村教师弱网环境、低配置设备，保障 PC 端 WEB 与手机端 H5 自适应流畅运行；

4. **指定技术栈落地**：数据库采用 Supabase，后端采用 Java 主流生态，前端采用业内成熟的轻量化响应式技术栈。

---

## 二、整体技术架构

采用**前后端分离的轻量化单体架构**，无分布式、微服务等复杂设计，最大化降低开发与运维成本，架构分层如下：

```mermaid
graph TD
    A[用户层] --> A1[PC端WEB浏览器]
    A --> A2[手机端H5浏览器]
    B[前端层] --> B1[Vue3+Vite 前端工程]
    B1 --> B2[Element Plus 组件库]
    B1 --> B3[响应式布局适配]
    C[网关层] --> C1[Nginx 反向代理]
    C1 --> D[后端服务层]
    D --> D1[SpringBoot 3.x 单体应用]
    D1 --> D2[Controller 接口层]
    D1 --> D3[Service 业务层]
    D1 --> D4[Mapper 数据访问层]
    D1 --> D5[Entity 数据实体层]
    E[数据层] --> E1[Supabase 托管PostgreSQL数据库]
    E1 --> E2[Supabase Auth 认证能力]
    E1 --> E3[RLS行级安全策略]```

---

## 三、前端技术详细设计

### 3\.1 前端核心技术栈选型

|技术选型|版本|选型原因|适配 MVP 的核心优势|
|---|---|---|---|
|核心框架|Vue 3\.4\.x \+ Vite 5\.x|业内主流轻量前端框架，组合式 API 开发效率高，Vite 构建速度快|单人开发上手快，代码量少，热更新快，大幅缩短开发周期|
|UI 组件库|Element Plus 2\.7\.x|适配 Vue3 的成熟 PC 端组件库，支持按需引入，自带响应式能力|内置表单、按钮、列表、弹窗等核心组件，无需重复造轮子，完全匹配极简风格设计|
|样式方案|SCSS \+ CSS 变量|预处理器简化样式开发，CSS 变量统一管理全局风格|可一键落地风格文档的配色、字号、圆角规范，全局统一修改成本极低|
|路由管理|Vue Router 4\.x|Vue 官方路由库，轻量稳定|匹配 15 个核心页面的跳转逻辑，配置简单，维护成本低|
|状态管理|Pinia 2\.x|Vue 官方轻量状态管理库|仅用于管理用户登录态、全局配置，代码量远小于 Vuex，适合单人开发|
|工具库|Axios \+ Lodash|业内标准 HTTP 请求库 \+ 工具函数库|简化接口请求、防抖、数据处理等通用逻辑，减少重复代码开发|
|适配方案|VW/VH \+ 媒体查询|原生 CSS 响应式方案，无第三方依赖|完美适配 PC 端与手机端 H5，一键实现风格文档要求的字号、留白自适应缩放|

### 3\.2 前端工程模块拆分

```Plain Text
src/
├── router/          # 路由模块（对应所有核心页面的路由配置）
├── views/           # 页面模块（1:1匹配PRD梳理的所有前端页面）
├── components/      # 通用组件模块（极简通用组件，无冗余设计）
├── api/             # 接口请求模块（按业务模块拆分接口）
├── store/           # 状态管理模块（仅用户登录态、全局配置）
├── assets/          # 静态资源模块（无图片/视频，仅全局样式文件）
├── utils/           # 工具函数模块（通用校验、防抖、复制/打印工具）
└── App.vue + main.js # 工程入口文件
```

#### 3\.2\.1 页面模块与路由映射（1:1 匹配前端页面清单）

|页面名称|路由地址|所属业务模块|核心权限|
|---|---|---|---|
|登录 / 注册页|/login|账号体系|免登录|
|首次登录昵称设置页|/init|账号体系|登录后未初始化昵称|
|个人备课列表页（首页）|/home|备课管理|登录后|
|我的班级列表页|/class|班级课程管理|登录后|
|创建复式班级页|/class/create|班级课程管理|登录后|
|班级详情页|/class/detail/:id|班级课程管理|登录后|
|添加课程页|/course/create/:classId|班级课程管理|登录后|
|纵向备课基础信息页|/prepare/vertical/base/:courseId|纵向备课|登录后|
|教学目标配置页|/prepare/vertical/target/:courseId|纵向备课|登录后|
|教学步骤与动静标记页|/prepare/vertical/step/:courseId|纵向备课|登录后|
|横向统筹首页|/prepare/horizontal/index/:courseId|横向统筹|登录后|
|时间轴表单修改页|/prepare/horizontal/time/:courseId|横向统筹|登录后|
|备课详情页（课堂卡片）|/prepare/detail/:courseId|备课结果输出|登录后|
|标准教案页|/prepare/plan/:courseId|备课结果输出|登录后|
|个人中心页|/user|账号体系|登录后|

#### 3\.2\.2 通用组件模块（仅保留 MVP 必需组件，无冗余设计）

|组件名称|功能说明|复用场景|
|---|---|---|
|PageContainer|页面通用容器，统一留白、标题、返回按钮|所有业务页面|
|FormItem|通用表单项，统一标签、输入框、错误提示样式|所有表单页面|
|StepList|教学步骤列表组件，支持新增 / 删除 / 排序|纵向备课步骤页|
|TimeAxisForm|时间轴表单项组件，支持时段新增 / 删除 / 校验|横向统筹时间轴页|
|StatusTag|备课状态标签，统一已备 / 未备样式|列表页、详情页|

### 3\.3 核心风格落地规范（1:1 匹配风格提示词）

通过全局 CSS 变量统一管理所有风格属性，确保全页面风格统一，开发零偏差：

```scss
// 全局CSS变量定义（assets/style/global.scss）
:root {
  // 配色规范（完全匹配风格提示词）
  --color-primary: #E6F4EA; // 主色调-浅草绿
  --color-primary-hover: #D4E9D9; // 按钮hover色
  --color-bg: #F8F7F4; // 浅米白-表单/列表背景
  --color-text-main: #333333; // 正文-深灰色
  --color-text-sub: #666666; // 辅助文字-浅灰色
  --color-success: #C8E6C9; // 已备课状态-淡绿色
  --color-danger: #F5C6C6; // 未备/警告-淡红色
  --color-border: #E0E0E0; // 边框-浅灰色

  // 字号规范（完全匹配风格提示词）
  --font-size-title: 20px; // 页面标题
  --font-size-module: 17px; // 模块标题
  --font-size-body: 15px; // 正文文字
  --font-size-btn: 16px; // 按钮/输入框文字
  --font-size-tip: 12px; // 辅助提示文字

  // 布局规范
  --pc-padding: 10%; // PC端左右留白
  --mobile-padding: 5%; // 手机端左右留白
  --border-radius: 2px; // 按钮圆角
  --spacing-sm: 5px; // 小间距
  --spacing-md: 10px; // 中间距
  --spacing-lg: 15px; // 大间距
  --spacing-xl: 20px; // 超大间距
}

// 媒体查询-手机端H5自适应
@media screen and (max-width: 768px) {
  :root {
    --font-size-title: 22px;
    --font-size-module: 18px;
    --font-size-body: 16px;
    --font-size-btn: 17px;
    --font-size-tip: 13px;
  }
}
```

#### 强制风格约束

1. 禁止使用任何自定义配色、字号，所有样式必须引用全局 CSS 变量；

2. 禁止使用渐变、阴影、动画等复杂效果，仅保留按钮 hover 的颜色变化；

3. 所有按钮、表单、列表必须严格匹配风格提示词的设计要求，无多余装饰；

4. 手机端通过媒体查询自动放大字号、调整留白，禁止横向滚动条出现。

### 3\.4 核心功能前端实现方案

|核心功能|实现方案|适配 MVP 的优化点|
|---|---|---|
|手机号验证码登录|对接 Supabase Auth 手机号登录接口，前端完成验证码发送、校验、登录态存储|无需后端开发认证逻辑，直接调用 Supabase 前端 SDK，缩短开发周期|
|表单自动保存|通过 Lodash 防抖函数，监听表单输入，300ms 无操作后自动调用保存接口，实时更新云端数据|匹配 PRD 实时自动保存要求，避免用户内容丢失，前端逻辑极简|
|复式模板一键生成时间轴|前端内置 2 套固定模板的时间分配规则，选择模板后自动生成时段数据，填充对应纵向备课的动 / 静环节素材|无复杂算法，固定规则匹配，开发量极小，完全满足 MVP 需求|
|动静冲突实时校验|表单修改时实时监听时段的起止时间、动环节数量，同一时段出现 2 个动环节即时标红提示，禁止提交|前端本地完成校验，减少后端接口请求，提升响应速度|
|一键复制全文|调用浏览器原生 Clipboard API，一键复制课堂卡片 / 教案的完整文本内容|无第三方依赖，原生 API 实现，兼容性好|
|教案打印|调用浏览器原生 window\.print \(\) API，单独编写打印媒体样式，隐藏页面非必要元素，适配 A4 纸张打印|原生实现，无需插件，完全满足教师打印教案的需求|
|弱网适配|接口请求超时重试机制，未联网时禁止编辑操作，提示网络异常，已编辑内容暂存 localStorage，联网后自动同步|适配乡村弱网环境，避免用户内容丢失|

---

## 四、后端技术详细设计

### 4\.1 后端核心技术栈选型

|技术选型|版本|选型原因|适配 MVP 的核心优势|
|---|---|---|---|
|核心框架|Spring Boot 3\.2\.x|Java 业内绝对主流的单体应用开发框架，内嵌 Tomcat，自动配置，开发效率极高|单人开发上手快，无需复杂配置，开箱即用，大幅缩短开发周期|
|接口规范|Spring MVC|Spring Boot 原生 Web 框架，标准化 RESTful 接口开发|业内通用规范，接口结构清晰，维护成本低|
|数据访问|MyBatis\-Plus 3\.5\.x|基于 MyBatis 的增强工具，内置 CRUD 接口，无需编写 XMLSQL|对接 Supabase PostgreSQL 数据库适配性极好，单表操作零 SQL 开发，代码量减少 80%|
|参数校验|Spring Validation|原生参数校验框架，注解式实现参数校验|简化接口入参校验，代码简洁，避免重复校验逻辑|
|工具集|Lombok \+ Hutool 5\.8\.x|Lombok 消除实体类 get/set 模板代码，Hutool 提供通用工具函数|大幅减少模板代码，提升开发效率，适合单人快速开发|
|认证鉴权|Supabase Auth \+ JJWT|基于 Supabase Auth 完成用户认证，JJWT 完成接口鉴权|无需开发完整的用户认证体系，仅需做 token 校验，开发量减少 70%|
|部署方式|Jar 包内嵌 Tomcat|无需单独部署 Web 容器，打包后直接运行|部署运维极简，适合单人维护|

### 4\.2 后端分层架构

采用经典的四层分层架构，职责清晰，代码结构简单，适合单人开发与维护：

```mermaid
graph TD
    A[前端请求] --> B[Controller 接口层]
    B --> C[Service 业务层]
    C --> D[Mapper 数据访问层]
    D --> E[Supabase PostgreSQL 数据库]
    F[通用能力层] --> B
    F --> C
    F --> F1[统一响应体]
    F --> F2[全局异常处理]
    F --> F3[接口鉴权拦截器]
    F --> F4[参数校验]```

#### 各层职责定义

1. **Controller 接口层**：负责接收前端 HTTP 请求，参数校验，调用业务层，返回统一响应结果，不包含任何业务逻辑；

2. **Service 业务层**：负责所有核心业务逻辑处理，是后端的核心层，按业务模块拆分，调用 Mapper 层完成数据操作；

3. **Mapper 数据访问层**：基于 MyBatis\-Plus 实现数据库的 CRUD 操作，仅负责数据读写，不包含业务逻辑；

4. **Entity 实体层**：与数据库表一一对应的实体类，通过 Lombok 简化代码；

5. **通用能力层**：负责全局异常处理、统一响应、接口鉴权、参数校验等通用能力，全项目复用。

### 4\.3 核心业务模块拆分与接口设计

严格匹配 MVP 功能边界，拆分 5 个核心业务模块，无冗余设计：

#### 4\.3\.1 通用规范定义

- 统一响应体格式

```java
@Data
public class Result<T> {
    private int code; // 200成功，非200失败
    private String msg; // 提示信息
    private T data; // 响应数据

    // 静态成功/失败方法
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMsg("操作成功");
        result.setData(data);
        return result;
    }

    public static <T> Result<T> fail(String msg) {
        Result<T> result = new Result<>();
        result.setCode(500);
        result.setMsg(msg);
        return result;
    }
}
```

- 全局错误码定义（极简，仅保留 MVP 必需）

    |    错误码|    含义|
    |---|---|
    |    200|    操作成功|
    |    401|    未登录 / Token 失效|
    |    403|    无权限操作|
    |    400|    参数校验失败|
    |    500|    服务器内部错误|

#### 4\.3\.2 核心业务模块与接口清单

|业务模块|核心接口|请求方式|接口地址|核心功能|
|---|---|---|---|---|
|用户认证模块|登录态校验|GET|/api/auth/check|校验用户 Token 有效性，获取用户信息|
|班级课程管理模块|创建复式班级|POST|/api/class/create|教师创建 2 级复式班级|
||班级列表查询|GET|/api/class/list|查询当前用户创建的所有班级|
||班级详情查询|GET|/api/class/detail/\{id\}|查询班级详情与关联课程列表|
||班级编辑 / 删除|PUT/DELETE|/api/class/\{id\}|编辑班级信息 / 删除班级|
||课程创建|POST|/api/course/create|为班级添加复式课程|
||课程详情查询|GET|/api/course/detail/\{id\}|查询课程详情与备课状态|
|纵向备课模块|纵向备课基础信息保存|POST|/api/prepare/vertical/base|保存 / 更新备课基础信息|
||教学目标保存|POST|/api/prepare/vertical/target|保存 / 更新 2 个年级的教学目标|
||教学步骤新增 / 编辑 / 删除|POST/PUT/DELETE|/api/prepare/vertical/step|教学步骤的增删改，标记动 / 静属性|
||纵向备课完整内容查询|GET|/api/prepare/vertical/\{courseId\}|查询课程对应的完整纵向备课内容|
|横向统筹模块|横向统筹内容保存|POST|/api/prepare/horizontal/save|保存时间轴配置、板书内容|
||横向统筹内容查询|GET|/api/prepare/horizontal/\{courseId\}|查询课程对应的横向统筹完整内容|
||备课完成提交|POST|/api/prepare/complete/\{courseId\}|标记课程为已备课，更新备课状态|
|备课结果管理模块|备课详情查询|GET|/api/prepare/detail/\{courseId\}|查询完整备课内容，生成课堂卡片 / 教案|
||个人备课列表查询|GET|/api/prepare/list|查询当前用户所有课程的备课列表与状态|
||备课内容删除|DELETE|/api/prepare/\{courseId\}|删除课程对应的备课内容，重置为未备课|

### 4\.4 核心业务逻辑实现方案

|核心业务|实现逻辑|适配 MVP 的优化点|
|---|---|---|
|用户数据隔离|所有业务表均关联 user\_id 字段，接口查询时强制过滤当前登录用户的 user\_id，仅返回用户自己创建的数据|无需复杂的权限体系，单字段过滤实现数据隔离，开发量极小，完全满足 MVP 需求|
|纵向备课自动保存|提供单字段增量保存接口，前端每次自动保存仅提交修改的字段，后端覆盖更新对应字段，无需全量提交|减少请求数据量，提升响应速度，避免全量覆盖导致的数据丢失|
|复式模板时间轴生成|后端内置 2 套固定模板的时间分配规则，提供模板规则查询接口，前端选择模板后直接套用规则生成时间轴|模板规则固定，无复杂动态算法，开发量极小|
|动静冲突后端兜底校验|备课完成提交时，后端二次校验时间轴数据，确保同一时段仅存在 1 个动环节，校验不通过禁止提交|前端校验 \+ 后端兜底双重保障，避免脏数据入库|
|备课状态同步|课程创建后默认状态为 0（未备课），完成横向统筹提交后更新为 1（已备课），删除备课内容后重置为 0|单字段状态标记，逻辑极简，全流程状态同步|

---

## 五、数据库设计（Supabase）

### 5\.1 Supabase 核心使用方案

1. **数据库**：使用 Supabase 官方托管的 PostgreSQL 15\+，无需自建数据库，零运维成本；

2. **认证**：使用 Supabase Auth 的手机号验证码登录能力，无需开发完整的认证体系，前端直接对接 SDK，后端仅做 Token 校验；

3. **安全策略**：使用 PostgreSQL 的 RLS 行级安全策略，确保用户仅能操作自己创建的数据，从数据库层面实现数据隔离，兜底接口层的权限控制；

4. **存储**：MVP 阶段无文件 / 图片上传需求，暂不启用 Supabase Storage，减少复杂度。

### 5\.2 核心数据表设计

#### 表 1：用户表（users，复用 Supabase Auth 内置用户表，扩展自定义字段）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|uuid|主键|Supabase Auth 生成的用户唯一 ID|
|phone|varchar|非空，唯一|用户手机号|
|nickname|varchar|非空|用户昵称|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|

#### 表 2：复式班级表（class\_info）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|bigint|主键，自增|班级唯一 ID|
|user\_id|uuid|非空，外键关联 \[users\.id\]\(users\.id\)|创建人用户 ID|
|class\_name|varchar|非空|班级名称（如：一二年级合班）|
|grade\_1|int|非空|合并的第一个年级（1\-6）|
|grade\_2|int|非空|合并的第二个年级（1\-6）|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|
|is\_deleted|tinyint|非空，默认 0|逻辑删除标记：0 未删除，1 已删除|

#### 表 3：课程表（course\_info）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|bigint|主键，自增|课程唯一 ID|
|class\_id|bigint|非空，外键关联 class\\\[\_info\.id\]\(\_info\.id\)|所属班级 ID|
|user\_id|uuid|非空，外键关联 \[users\.id\]\(users\.id\)|创建人用户 ID|
|course\_name|varchar|非空|课程名称|
|course\_duration|int|非空，默认 45|课时时长（40/45 分钟）|
|grade\_1\_subject|varchar|非空|第一个年级的学科|
|grade\_2\_subject|varchar|非空|第二个年级的学科|
|prepare\_status|tinyint|非空，默认 0|备课状态：0 未备课，1 已备课|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|
|is\_deleted|tinyint|非空，默认 0|逻辑删除标记：0 未删除，1 已删除|

#### 表 4：纵向备课主表（prepare\_vertical）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|bigint|主键，自增|纵向备课唯一 ID|
|course\_id|bigint|非空，外键关联 course\\\[\_info\.id\]\(\_info\.id\)|所属课程 ID|
|user\_id|uuid|非空，外键关联 \[users\.id\]\(users\.id\)|创建人用户 ID|
|grade\_1\_book\_version|varchar||第一个年级教材版本|
|grade\_1\_unit|varchar||第一个年级单元|
|grade\_1\_lesson|varchar||第一个年级课时|
|grade\_2\_book\_version|varchar||第二个年级教材版本|
|grade\_2\_unit|varchar||第二个年级单元|
|grade\_2\_lesson|varchar||第二个年级课时|
|grade\_1\_target|text||第一个年级教学目标|
|grade\_2\_target|text||第二个年级教学目标|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|

#### 表 5：教学步骤表（prepare\_step）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|bigint|主键，自增|步骤唯一 ID|
|prepare\_id|bigint|非空，外键关联 prepare\\\[\_vertical\.id\]\(\_vertical\.id\)|所属纵向备课 ID|
|course\_id|bigint|非空，外键关联 course\\\[\_info\.id\]\(\_info\.id\)|所属课程 ID|
|user\_id|uuid|非空，外键关联 \[users\.id\]\(users\.id\)|创建人用户 ID|
|grade|int|非空|所属年级|
|step\_name|varchar|非空|步骤名称|
|step\_content|text|非空|步骤教学内容|
|step\_type|tinyint|非空|环节类型：1 动环节，2 静环节|
|duration|int|非空|步骤预计时长（分钟）|
|sort|int|非空，默认 0|步骤排序号|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|
|is\_deleted|tinyint|非空，默认 0|逻辑删除标记：0 未删除，1 已删除|

#### 表 6：横向统筹主表（prepare\_horizontal）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|bigint|主键，自增|横向统筹唯一 ID|
|course\_id|bigint|非空，外键关联 course\\\[\_info\.id\]\(\_info\.id\)|所属课程 ID|
|user\_id|uuid|非空，外键关联 \[users\.id\]\(users\.id\)|创建人用户 ID|
|template\_type|tinyint|非空|使用的模板类型：1 先低后高，2 先高后低|
|grade\_1\_blackboard|text||第一个年级板书内容|
|grade\_2\_blackboard|text||第二个年级板书内容|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|

#### 表 7：时间轴时段表（prepare\_time\_axis）

|字段名|类型|约束|字段说明|
|---|---|---|---|
|id|bigint|主键，自增|时段唯一 ID|
|horizontal\_id|bigint|非空，外键关联 prepare\\\[\_horizontal\.id\]\(\_horizontal\.id\)|所属横向统筹 ID|
|course\_id|bigint|非空，外键关联 course\\\[\_info\.id\]\(\_info\.id\)|所属课程 ID|
|user\_id|uuid|非空，外键关联 \[users\.id\]\(users\.id\)|创建人用户 ID|
|start\_minute|int|非空|时段开始分钟数|
|end\_minute|int|非空|时段结束分钟数|
|active\_grade|int||动环节年级，无则为 null|
|active\_content|text||动环节教学内容|
|static\_grade\_1\_content|text||第一个年级静环节任务|
|static\_grade\_2\_content|text||第二个年级静环节任务|
|sort|int|非空，默认 0|时段排序号|
|created\_at|timestamptz|非空|创建时间|
|updated\_at|timestamptz|非空|更新时间|
|is\_deleted|tinyint|非空，默认 0|逻辑删除标记：0 未删除，1 已删除|

### 5\.3 RLS 行级安全策略设置

为所有业务表启用 RLS 策略，确保数据安全，核心策略示例：

```sql
-- 班级表RLS策略：用户仅能操作自己创建的班级
CREATE POLICY class_user_isolate ON class_info
FOR ALL USING (auth.uid() = user_id);

-- 课程表RLS策略
CREATE POLICY course_user_isolate ON course_info
FOR ALL USING (auth.uid() = user_id);

-- 所有备课相关表均设置相同的用户隔离策略
```

---

## 六、部署与运维方案

### 6\.1 部署架构（极简，适合单人维护）

```mermaid
graph TD
    A[用户访问] --> B[云服务器 2核4G  CentOS 7.9]
    B --> C[Nginx 1.24.x]
    C --> C1[前端静态资源]
    C --> C2[反向代理至后端服务]
    C2 --> D[SpringBoot Jar包 后端服务]
    D --> E[Supabase 云端PostgreSQL数据库]```

### 6\.2 部署步骤

1. **前端部署**：执行`npm run build`打包前端工程，生成 dist 目录，将 dist 目录下的静态文件上传至云服务器 Nginx 的 html 目录，配置 Nginx 静态资源访问与路由转发；

2. **后端部署**：通过 Maven 打包 SpringBoot 工程，生成可执行 Jar 包，上传至云服务器，通过`nohup java \-jar xxx\.jar \&amp;`命令后台运行，配置开机自启；

3. **数据库**：直接使用 Supabase 官方托管服务，无需部署运维，仅需在 Supabase 控制台创建表、配置 RLS 策略即可。

### 6\.3 环境配置

- 开发环境：本地开发，连接 Supabase 开发环境实例，前后端本地联调；

- 生产环境：云服务器部署，连接 Supabase 生产环境实例，配置 Nginx HTTPS 证书，保障访问安全。

---

## 七、开发排期适配（1 人月 / 4 周）

|开发周期|前后端开发内容|交付成果|
|---|---|---|
|第 1 周|前端：工程搭建、路由配置、登录 / 注册 / 个人中心页面开发<br>后端：工程搭建、数据库表创建、用户认证 / 班级课程管理模块接口开发|1\. 前后端工程基础框架搭建完成<br>2\. 账号体系、班级课程管理全功能开发完成<br>3\. Supabase 表结构与 RLS 策略配置完成|
|第 2 周|前端：纵向备课全流程页面开发、通用组件开发<br>后端：纵向备课模块接口开发、自动保存逻辑实现|1\. 纵向备课全流程页面开发完成<br>2\. 纵向备课模块接口开发完成，前后端联调通过<br>3\. 全局风格规范落地完成|
|第 3 周|前端：横向统筹全流程页面开发、动静冲突校验实现<br>后端：横向统筹模块接口开发、备课状态同步逻辑实现|1\. 横向统筹全流程页面开发完成<br>2\. 核心业务逻辑开发完成，前后端联调通过<br>3\. 复制 / 打印 / 自适应适配功能完成|
|第 4 周|前端：备课结果输出、个人备课列表页面开发、全流程测试 bug 修复<br>后端：备课结果管理模块接口开发、全流程接口测试、bug 修复<br>全流程联调、部署上线|1\. 全业务流程闭环开发完成<br>2\. 所有 bug 修复完成，功能符合 PRD 要求<br>3\. 生产环境部署完成，可正常访问使用|

> （注：文档部分内容可能由 AI 生成）
